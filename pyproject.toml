[project]
name = "linode-mcp"
version = "0.1.0"
description = "MCP server for managing Linode/Akamai Compute infrastructure via natural language"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.8.0",
    "httpx>=0.27.0",
    "pydantic>=2.0",
    "boto3>=1.35.0",
]
